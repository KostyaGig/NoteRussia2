<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kostya.noterussia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteRussia">
        <activity
            android:name=".main_screens.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.NoteRussia.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".details_screens.AddNoteActivity" android:theme="@style/Theme.NoteRussia"/>
    </application>

</manifest>